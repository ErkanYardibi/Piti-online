IPHONEOS_DEPLOYMENT_TARGET = 16.0
SWIFT_VERSION = 5.0
MARKETING_VERSION = 1.0.0
CURRENT_PROJECT_VERSION = 1
PITI_PRODUCTION_URL = https:/$()/mypiti.online
// Must be a separately verified non-production deployment. No fallback.
PITI_STAGING_URL =
// Optional, ignored local file: signing/team and verified staging URL only.
#include? "Local.xcconfig"
