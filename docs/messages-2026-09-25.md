# Messaging update — 25 September 2026

Explicit request: publish this message-screen change to both staging and production.

- Sticker (PT only), emoji, photo, audio and video use accessible icon buttons. Paperclip adds PDF, Word, Excel, PowerPoint, TXT, CSV or ZIP documents, up to 15 MiB.
- Private `chat-media` storage and existing PT/member access policies remain in place. Documents retain their original display filename and use signed download links.
- The conversation has its own scrolling area; the composer stays outside it. Opening a conversation shows the latest messages. Reading older messages preserves position during refresh; the down arrow and sending return to the latest message.
- Fetch the newest 100 messages first; scrolling upwards loads earlier pages using a timestamp/id cursor. Multiline input grows to a capped height. Shift+Enter adds a line.

Validation: chat-layout-files.cjs, chat-read-retention.cjs, demo-messaging.cjs and staging-bundle.test.mjs passed. chat-files.sql passed on staging using a rolled-back temporary table, including invalid size/type/name/path checks. Existing chat-tasks-ui.cjs has an unrelated pre-existing authentication mock failure, reproduced against unmodified staging source; it was not changed. Shared demo task assignment/completion and receipt tests pass.

The cloud browser cannot access local previews. The published staging login page is reachable, but authenticated visual acceptance and a physical phone/keyboard check remain unverified. No claim of full device acceptance is made.

Production preparation: active production HTML matched feature/ios-v1 commit 6c54bb85f90c6b261b95f458e532e383bd5c8158 exactly after worker.js runtime transformations. main still pointed at older 565b01c. Production frontend source/assets are synchronized to the already-active version before applying this change; production Wrangler configuration stays intact. Staging is never merged wholesale into main.

Rollback: revert the message UI changes while retaining the verified active production source. The additive file schema can remain; do not remove it after files have been sent. No user data is reset by this release.

## Full-screen conversation follow-up

- Message navigation now opens a dedicated viewport. The app header, sidebar/bottom navigation and floating demo launcher are hidden only while chatting. The home icon restores the normal page layout.
- Student selection stays in the compact header. Archive inclusion, retention information and demo user switching move into the three-dot options panel.
- VisualViewport height and top offset keep the composer within the available screen; safe-area padding is included. This is an app layout, not a request to hide browser chrome.
- The task assignment checkbox is an accessible toolbar icon. Existing messages use a small task-conversion icon in the metadata footer; conversion still opens the existing review dialog.
- Task bubbles use a small icon, a title without duplicated body text, inline status/due date and result. Result photographs expand on request. Member completion remains available and role rules are unchanged.
- Targeted checks cover entering/exiting chat, restored global navigation, access to archived conversations, icon labels, task assignment/conversion/completion and existing history/file/receipt behavior. No schema or user-data changes.
